# Mercado Pago Provider Simulator

Status: first create/retrieve increment built in the shared simulation.

The implementation preserves the Orders API shape: order containing a payment
transaction, decimal-string amounts, Pix QR fields, and native status pairs.
See [scenarios](scenarios.md) and the provider research under
`research/mercadopago/`.
