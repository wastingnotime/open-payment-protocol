# Asaas Research

Use `../template.md` before adding provider claims or sanitized fixtures.
Preserve Asaas terminology and behavior.

## Source Access

The official Asaas documentation MCP at `https://docs.asaas.com/mcp` is the
primary discovery interface and is configured in `.codex/config.toml`. Claims
still cite the official page or OpenAPI endpoint returned by the MCP and record
the access date.

Current research covers the creation and retrieval portion of the first
provider-native Pix slice. It is documentation evidence only; no Asaas Sandbox
interaction has been recorded.
