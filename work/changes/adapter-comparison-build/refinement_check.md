# Thin adapter comparison refinement check

The first comparison report is intentionally narrow: it combines current
scenario inventories with the five provider-native resource, QR, and money
boundaries recorded in `research/comparison.md`. It does not emit a common
payment object, status enum, money type, or webhook contract.

The report is discovery evidence for future adapter experiments, not an OPP
implementation or conformance surface.
