# Iugu webhook events refinement — cycle 2 build

- Added transport and event-only boundary checks for all four new Iugu events.
- Full deterministic suite passed (`140 passed`).
- Validation passed with 84 scenarios.
