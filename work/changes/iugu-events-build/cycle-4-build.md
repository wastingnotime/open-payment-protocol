# Iugu webhook events refinement — cycle 4 build

- Added four Iugu event-only nodes and topology edges to the native graph.
- Graph now exposes 77 nodes, 116 routes, 39 known edges, and 2 deferred edges.
- Full deterministic suite passed (`141 passed`).
