# Iugu webhook events refinement — cycle 8 build

- Added checks that event-only webhook scenarios do not invent invoice state transitions.
- Full deterministic suite passed (`144 passed`).
- Validation passed with 84 scenarios.
