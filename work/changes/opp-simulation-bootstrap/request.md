# OPP Simulation Bootstrap Request

## Request

Start the domain simulation using the repository's 2026-08-09 Codex handoff and
WNT MRL simulation guidance.

## Interpreted Scope

Bootstrap the simulation surface, extract a non-authoritative semantic baseline,
map candidate slices, and refine the first candidate far enough to expose its
evidence gate. Do not implement provider behavior before provider research and
sanitized fixtures support it.

## Source

- Original handoff supplied by the user from
  `~/Downloads/open-payment-protocol-codex-handoff.md`.
- Repository guidance in `AGENTS.md`, `README.md`, `contracts/README.md`, and
  `research/template.md`.

The handoff is not copied into the repository because it includes broad project
planning beyond this bounded change. This record preserves its identity and the
specific decisions extracted into repository-owned semantic artifacts.
