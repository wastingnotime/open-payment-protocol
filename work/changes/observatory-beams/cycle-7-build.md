# Observatory beams — cycle 7 build

- Refinement: the MRL adapter now registers every graph actor and uses the
  declared Iugu actor ID for the scheduled discovery action.
- Build: pure tests plus the installed MRL runtime adapter/runner.
- Result: 73 passed; runtime scenario exposes 6 actors, 43 nodes, 56 edges,
  and 56 graph-route observations.
