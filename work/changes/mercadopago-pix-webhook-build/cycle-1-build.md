# Mercado Pago Pix webhook lifecycle — cycle 1 build

- Added `MP-PIX-011` for a processing order notification.
- The scenario preserves JSON order notification shape, GET reconciliation,
  and HMAC signature verification.
- Build: focused Mercado Pago tests passed (`12 passed`).
