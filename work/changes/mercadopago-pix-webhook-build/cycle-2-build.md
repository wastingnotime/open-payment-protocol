# Mercado Pago Pix webhook lifecycle — cycle 2 build

- Added `MP-PIX-012` for mismatched `x-signature` rejection and discard.
- Build: full deterministic suite passed (`92 passed`); 66 scenarios validated.
