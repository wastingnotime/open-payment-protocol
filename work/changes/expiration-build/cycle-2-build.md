# Expiration refinement — cycle 2 build

- Added `MP-PIX-015` for unpaid cancellation.
- Added `MP-PIX-016` and `MP-PIX-017` for the documented `expired`/`canceled` expiration alternatives.
- Provider inventory now contains 77 scenarios.
- Full deterministic suite passed (`124 passed`).
