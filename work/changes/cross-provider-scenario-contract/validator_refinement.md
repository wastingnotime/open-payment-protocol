# Scenario Validator Refinement

The standalone scenario validator is the command-line counterpart to the
cross-provider test contract. Its checks must remain limited to report shape,
observation attribution, and the repository's explicit cardholder-data
negative boundary.
