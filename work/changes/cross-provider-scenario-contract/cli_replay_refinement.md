# CLI Replay Refinement

The standalone validator must run each provider registry twice and compare
canonicalized report snapshots. This keeps the command-line receipt aligned
with the deterministic replay test.
