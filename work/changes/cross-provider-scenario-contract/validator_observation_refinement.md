# Validator Observation Refinement

The command-line validator must enforce the same observation-envelope contract
as the deterministic test suite. This keeps local report validation useful
outside pytest without imposing a shared provider payload schema.
