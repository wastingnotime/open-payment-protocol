# Observation Envelope Refinement

Cross-provider comparison needs a stable envelope for observations while
preserving native provider payloads. The next build checks the four envelope
fields (`type`, `name`, `source`, and `payload`) plus scenario attribution.
