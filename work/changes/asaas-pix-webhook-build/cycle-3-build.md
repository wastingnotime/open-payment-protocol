# Asaas Pix webhook lifecycle — cycle 3 build

- Added `AS-PIX-011` for the documented `PAYMENT_OVERDUE` event sequence.
- The scenario explicitly avoids inferring a response-status transition.
- Build: full deterministic suite passed (`98 passed`); 69 scenarios validated.
