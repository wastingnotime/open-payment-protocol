# Asaas Pix webhook lifecycle — cycle 1 build

- Added `AS-PIX-009` for a documented `PAYMENT_RECEIVED` envelope.
- The scenario preserves event identity and the persist-before-ack boundary.
- Build: focused Asaas tests passed (`9 passed`).
