# Asaas Pix webhook lifecycle — cycle 9 build

- Notification tests now require the complete native payment object in the
  envelope, including the `PIX` billing type.
- Build: full deterministic suite passed (`101 passed`).
