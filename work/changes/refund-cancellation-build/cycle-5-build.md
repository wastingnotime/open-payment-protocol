# Refund and cancellation refinement — cycle 5 build

- Expanded the native graph with four executable refund/cancellation nodes and four lifecycle edges.
- Graph now exposes 67 nodes, 103 routes, 36 known edges, and 2 deferred edges.
- Full deterministic suite passed (`117 passed`).
